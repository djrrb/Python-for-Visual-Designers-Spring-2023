# draw a rectangle the height of the canvas
# rect(x, y, width, height)
rect(0, 0, 100, height())

# make the color green
fill(0, 1, 0)

# draw a circle half the height and width of the canvas, slightly offset to the right
# oval(x, y, width, height)
oval(200, 0, width()/2, height()/2)